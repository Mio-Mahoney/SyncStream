node_modules

# Output
.output
.vercel
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test
.PLAN.md

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright writes and wipes this on every run. The directory is `test-results`,
# not `tests-results`, and the typo let 1300+ traces get committed - and left the
# dir mid-rewrite under `git add -A`, which fails outright on a file playwright
# deleted between the stat and the add.
/test-results

# Generated on demand by tests/fixtures/gen.sh --large (PLAN.md 7, Phase 0)
tests/fixtures/large-2gb.mp4
